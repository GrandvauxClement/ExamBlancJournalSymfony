<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('articles') }}"><img src="{{ asset('images/dauphine.jpg') }}" style="max-width: 150px"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('articles') }}"> La liste d'article  </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin') }}">Accès admin </a>
            </li>
            {% if user != null %}
                <li class="nav-item ">
                    <a class="nav-link text-danger" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>